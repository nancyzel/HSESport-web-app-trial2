@{
    ViewData["Title"] = "Информация о студенте";
    Layout = "_Layout_Student";

}
<h1>@ViewData["Title"]</h1>

<div class="wrapper" style="justify-content: space-between;">
    <div class="col-md-4">
        <div class="card p-3 shadow-sm bg-light rounded">
            <div class="d-flex align-items-center mb-2">
                    <p><strong>Фамилия:</strong> @ViewBag.StudentSurname</p>
                    <p><strong>Имя:</strong> @ViewBag.StudentName</p>
                    <p><strong>Отчество:</strong> @ViewBag.StudentSecondName</p>
            </div>
            <a href="#" class="btn btn-primary w-100 mb-2">Редактировать</a>
            <p class="mb-0"><strong>Email:</strong> @ViewBag.StudentEmail</p>
        </div>
    </div>
    <div class="col-md-8" style="align-items: flex-start">
        <div class="card p-3 shadow-sm bg-light rounded">
            <div class="mt-4">
                <p class="mb-0"><strong>Общее количество посещений: </strong>@ViewBag.StudentAttendanceRate</p>
                <p><strong>Посещаемые секции:</strong></p>
                @if (ViewBag.SectionNames != null && ((List<string>)ViewBag.SectionNames).Any())
                {
                    <ul>
                        @foreach (var sectionName in ViewBag.SectionNames)
                        {
                            <li>@sectionName</li>
                        }
                    </ul>
                }
                else
                {
                    <p>У студента нет закрепленных секций.</p>
                }
            </div>
        </div>
    </div>
</div>